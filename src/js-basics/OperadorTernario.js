// Operador Ternário

const pagamento = 100;

// Condição ? resultado se condição true : resultado se condição false
let juros = pagamento < 100 ? pagamento : pagamento + pagamento * 0.1;
console.log(juros); 